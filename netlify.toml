[build]
  command = "npm ci && npm run build"
  publish = "dist"

[context.production.environment]
  # Optional: force Node version used by Netlify (uncomment and set if needed)
  # NODE_VERSION = "18"
