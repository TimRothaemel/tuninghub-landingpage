[build]
  # Use `npm install` on Netlify since `npm ci` requires a package-lock.json
  command = "npm install && npm run build"
  publish = "dist"

[context.production.environment]
  # Optional: force Node version used by Netlify (uncomment and set if needed)
  # NODE_VERSION = "18"
